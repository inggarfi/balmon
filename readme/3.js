(function(global){function crc16ccitt(str) { let crc = 0xFFFF; for (let i = 0; i < str.length; i++) { crc ^= str.charCodeAt(i) << 8; for (let j = 0; j < 8; j++) { crc = (crc & 0x8000) ? ((crc << 1) ^ 0x1021) : (crc << 1); crc &= 0xFFFF; } function generateDynamicQRIS(staticQRIS, amount) { let qris = staticQRIS.replace("010211", "010212"); qris = qris.replace(/540\d{2};global.generateDynamicQRIS=generateDynamicQRIS;})(window);